import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#36343496',
        alignItems: 'center',
        justifyContent: 'center',
    },
    textInfo: {
        fontSize: 30,
        color: 'red'
    }
});

export default styles;